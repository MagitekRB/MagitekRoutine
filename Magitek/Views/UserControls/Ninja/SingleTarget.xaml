<UserControl x:Class="Magitek.Views.UserControls.Ninja.SingleTarget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:properties="clr-namespace:Magitek.Properties"
             xmlns:controls="clr-namespace:Magitek.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Magitek.ViewModels"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:enums="clr-namespace:Magitek.Enumerations"
             d:DesignHeight="400"
             d:DesignWidth="500"
             mc:Ignorable="d">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Magitek;component/Styles/Magitek.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <ObjectDataProvider x:Key="InterruptStrategy" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="enums:InterruptStrategy" />
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <controls:SettingsBlock Margin="0,2" Background="{DynamicResource ClassSelectorBackground}">
                <StackPanel Margin="5" Orientation="Horizontal">
                    <CheckBox Content="{x:Static properties:Resources.Generic_Use_Interrupt_And_Stun_If_Activated}" IsChecked="{Binding NinjaSettings.UseStunOrInterrupt, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    <ComboBox Width="170" ItemsSource="{Binding Source={StaticResource InterruptStrategy}}" SelectedValue="{Binding NinjaSettings.Strategy, Mode=TwoWay}" Style="{DynamicResource ComboBox}" />
                </StackPanel>
            </controls:SettingsBlock>

            <controls:SettingsBlock Margin="0,2" Background="{DynamicResource ClassSelectorBackground}">
                <StackPanel Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="0,3" Content="{x:Static properties:Resources.Generic_Hide_Positional_Message}" IsChecked="{Binding NinjaSettings.HidePositionalMessage, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                </StackPanel>
            </controls:SettingsBlock>

            <controls:SettingsBlock Margin="0,2" Background="{DynamicResource ClassSelectorBackground}">
                <StackPanel Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_Assassinate}" IsChecked="{Binding NinjaSettings.UseAssassinate, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_TenChiJin}" IsChecked="{Binding NinjaSettings.UseTenChiJin, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_Mug}" IsChecked="{Binding NinjaSettings.UseMug, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_but_do_Not_Use_If_Enemy_Is_Dying_Within}" />
                        <controls:Numeric  MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.DontMugIfEnemyDyingWithinSeconds, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Generic_Seconds}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_TrickAttack}" IsChecked="{Binding NinjaSettings.UseTrickAttack, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_but_do_Not_Use_If_Enemy_Is_Dying_Within}" />
                        <controls:Numeric  MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.DontTrickAttackIfEnemyDyingWithinSeconds, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Generic_Seconds}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_Bhavacakra}" IsChecked="{Binding NinjaSettings.UseBhavacakra, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_ZeshoMeppo}" IsChecked="{Binding NinjaSettings.UseZeshoMeppo, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Forked_Raiju}" IsChecked="{Binding NinjaSettings.UseForkedRaiju, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <CheckBox Margin="5" Content="{x:Static properties:Resources.Ninja_Content_Use_ThrowingDagger}" IsChecked="{Binding NinjaSettings.UseThrowingDagger, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                </StackPanel>
            </controls:SettingsBlock>

            <controls:SettingsBlock Margin="0,2" Background="{DynamicResource ClassSelectorBackground}">
                <StackPanel Margin="5">
                    <TextBlock Margin="0,0,0,5" Style="{DynamicResource TextBlockSection}" Text="{x:Static properties:Resources.Generic_Advance_Burst_Window_Settings}" />
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Generic_Hold_Burst}" IsChecked="{Binding NinjaSettings.BurstLogicHoldBurst, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                </StackPanel>
            </controls:SettingsBlock>

        </StackPanel>
    </ScrollViewer>
</UserControl>
