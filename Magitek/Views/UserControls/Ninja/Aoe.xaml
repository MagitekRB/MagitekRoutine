<UserControl x:Class="Magitek.Views.UserControls.Ninja.Aoe"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Magitek.Properties"
             xmlns:controls="clr-namespace:Magitek.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Magitek.ViewModels"
             d:DesignHeight="400"
             d:DesignWidth="500"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <Binding Source="{x:Static viewModels:BaseSettings.Instance}" />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary Source="/Magitek;component/Styles/Magitek.xaml" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <controls:SettingsBlock Margin="0,5,0,0" Background="{DynamicResource ClassSelectorBackground}">
                <StackPanel Margin="3">
                    <TextBlock Margin="5,5,5,0" Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Aoe_Tab_Header}" TextWrapping="Wrap" />
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_AoeCombo}" IsChecked="{Binding NinjaSettings.UseAoeCombo, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.AoeEnemies, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Enemies_Within_5_Of_You_One_Fewer_Under_Doton}" />
                    </StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_Katon}" IsChecked="{Binding NinjaSettings.UseKaton, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.KatonEnemies, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Enemies_Within_5_Of_Target_One_Fewer_Under_Doton}" />
                    </StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_Doton}" IsChecked="{Binding NinjaSettings.UseDoton, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.DotonEnemies, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Enemies_Within_5_Of_Target_Standing_Still}" />
                    </StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_Huton}" IsChecked="{Binding NinjaSettings.UseHuton, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.HutonEnemies, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Enemies_Within_5_Of_You}" />
                    </StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_GokaMekkyaku}" IsChecked="{Binding NinjaSettings.UseGokaMekkyaku, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.GokaMekkyakuEnemies, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Enemies_Within_5_Of_Target}" />
                    </StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_HellfrogMedium}" IsChecked="{Binding NinjaSettings.UseHellfrogMedium, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                        <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding NinjaSettings.HellfrogMediumEnemies, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Ninja_Text_Enemies_One_More_Under_Meisui}" />
                    </StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.Ninja_Content_Use_TenriJindo}" IsChecked="{Binding NinjaSettings.UseTenriJindo, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                </StackPanel>
            </controls:SettingsBlock>

        </StackPanel>
    </ScrollViewer>
</UserControl>