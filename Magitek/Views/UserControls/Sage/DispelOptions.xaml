<UserControl x:Class="Magitek.Views.UserControls.Sage.DispelOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Magitek.Properties"
             xmlns:controls="clr-namespace:Magitek.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Magitek.ViewModels"
             d:DesignHeight="400" d:DesignWidth="500" mc:Ignorable="d">

    <UserControl.DataContext>
        <Binding Source="{x:Static viewModels:BaseSettings.Instance}" />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary Source="/Magitek;component/Styles/Magitek.xaml" />
    </UserControl.Resources>

    <StackPanel Margin="10">
        <controls:SettingsBlock Background="{DynamicResource ClassSelectorBackground}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <CheckBox Content="{x:Static properties:Resources.Generic_Use_Esuna_To_Dispel}" IsChecked="{Binding SageSettings.Dispel, Mode=TwoWay}"
               Style="{DynamicResource CheckBoxFlat}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <CheckBox Content="{x:Static properties:Resources.Generic_Dispel_Anything_That_Is_Removable}"
               IsChecked="{Binding SageSettings.AutomaticallyDispelAnythingThatsDispellable, Mode=TwoWay}"
               Style="{DynamicResource CheckBoxFlat}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <CheckBox Content="{x:Static properties:Resources.Sage_Content_Dispel_Only_If_Nearby_Players_Health_Is_Above}"
               IsChecked="{Binding SageSettings.DispelOnlyAbove, Mode=TwoWay}"
               Style="{DynamicResource CheckBoxFlat}" />
                    <controls:Numeric Margin="5,0" Value="{Binding SageSettings.DispelOnlyAboveHealth, Mode=TwoWay}" />
                    <TextBlock Text="Percent" VerticalAlignment="Center" />
                </StackPanel>
            </StackPanel>
        </controls:SettingsBlock>
    </StackPanel>
</UserControl>
