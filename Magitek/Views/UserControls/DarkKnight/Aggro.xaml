<UserControl x:Class="Magitek.Views.UserControls.DarkKnight.Aggro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Magitek.Properties"
             xmlns:controls="clr-namespace:Magitek.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Magitek.ViewModels"
             d:DesignHeight="400"
             d:DesignWidth="500"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <Binding Source="{x:Static viewModels:BaseSettings.Instance}" />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary Source="/Magitek;component/Styles/Magitek.xaml" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
        <StackPanel Margin="5" Orientation="Horizontal">
            <CheckBox Grid.Column="0" Content="{x:Static properties:Resources.DarkKnight_Content_Grit_Tank_Stance}" IsChecked="{Binding DarkKnightSettings.Grit, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
            <CheckBox Margin="5,0,0,0" Content="{x:Static properties:Resources.Generic_Manually_Control_Tank_Stance}" IsChecked="{Binding DarkKnightSettings.ManuallyControlTankStance, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
        </StackPanel>

        <controls:SettingsBlock Margin="0,5" Background="{DynamicResource ClassSelectorBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="{x:Static properties:Resources.DarkKnight_Content_Use_Unmend_if_not_in_melee_to_damage}" IsChecked="{Binding DarkKnightSettings.UnmendToDps, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Margin="0,3,0,0" Content="{x:Static properties:Resources.DarkKnight_Content_Use_Unmend_To_Pull_or_Regain_Aggro}" IsChecked="{Binding DarkKnightSettings.UnmendToPullOrAggro, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.DarkKnight_Text_Use_Unmend_When_Ennemy_Is_At_Least_Yalms_Away}"  />
                        <controls:Numeric Margin="0,3" MaxValue="15" MinValue="0" Value="{Binding DarkKnightSettings.UnmendMinDistance, Mode=TwoWay}" />
                        <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Generic_Yalms_Away}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Margin="5">
                </StackPanel>
            </Grid>
        </controls:SettingsBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>