<UserControl x:Class="Magitek.Views.UserControls.Scholar.Aetherpact"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Magitek.Properties" 
             xmlns:controls="clr-namespace:Magitek.Controls" 
             xmlns:viewModels="clr-namespace:Magitek.ViewModels">

    <UserControl.DataContext>
        <Binding Source="{x:Static viewModels:BaseSettings.Instance}" />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary Source="/Magitek;component/Styles/Magitek.xaml" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

        <controls:SettingsBlock Background="{DynamicResource ClassSelectorBackground}">
            <StackPanel Margin="5">

                <CheckBox Content="{x:Static properties:Resources.Generic_Aetherpact}" IsChecked="{Binding ScholarSettings.Aetherpact, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />

                <StackPanel Margin="0,3" Orientation="Horizontal">
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Scholar_Text_When_The_Fairie_Gauge_Is_At_Least}" />
                    <controls:Numeric MaxValue="100" Increment="10" MinValue="10" Value="{Binding ScholarSettings.AetherpactMinimumFairieGauge, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel Margin="0,3" Orientation="Horizontal">
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Scholar_Text_When_The_Tank_Is_Below}" />
                    <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding ScholarSettings.AetherpactHealthPercent, Mode=TwoWay}" />
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Generic_Health_Percent}" />
                </StackPanel>

                <StackPanel Margin="0,3" Orientation="Horizontal">
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Scholar_Text_Break_Aetherpact_If_Tank_Is_Full_HP_O}" />
                    <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding ScholarSettings.AetherpactEnemies, Mode=TwoWay}" />
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Scholar_Text_or_less_Enemies}" />
                </StackPanel>

            </StackPanel>
        </controls:SettingsBlock>

        <controls:SettingsBlock Margin="0,5" Background="{DynamicResource ClassSelectorBackground}">
            <StackPanel Margin="5">

                <StackPanel Margin="0,3" Orientation="Horizontal">
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Scholar_Text_When_The_Tank_Has_Aetherpact_Only_Phy}" />
                    <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding ScholarSettings.AetherpactPhysickHealthPercent, Mode=TwoWay}" />
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Generic_Health_Percent}" />
                </StackPanel>

            </StackPanel>
        </controls:SettingsBlock>

        <controls:SettingsBlock Margin="0,5" Background="{DynamicResource ClassSelectorBackground}">
            <StackPanel Margin="5">

                <CheckBox Content="{x:Static properties:Resources.Scholar_Content_Use_Adloquium_On_Tank_If_They_Have}" IsChecked="{Binding ScholarSettings.AetherpactUseAdloquium, Mode=TwoWay}" Style="{DynamicResource CheckBoxFlat}" />

                <StackPanel Margin="0,3" Orientation="Horizontal">
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Scholar_Text_Only_If_They_Are_Below}" />
                    <controls:Numeric MaxValue="100" MinValue="1" Value="{Binding ScholarSettings.AetherpactAdloquiumHealthPercent, Mode=TwoWay}" />
                    <TextBlock Style="{DynamicResource TextBlockDefault}" Text="{x:Static properties:Resources.Generic_Health_Percent}" />
                </StackPanel>


            </StackPanel>
        </controls:SettingsBlock>        

        </StackPanel>
    </ScrollViewer>
</UserControl>
